# greet プロジェクト固有ルール

- プロダクトの why は `product-brief.md`。全判断の基準。
- PDH 汎用 agent ルールは `PDH-AGENTS.md`、運用ルールは `docs/product-delivery-hierarchy.md`。
- 実装は `src/greet.py` の 1 ファイルに収める。テストは `python3 -m pytest` か素の実行で確認する。

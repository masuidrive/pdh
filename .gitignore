.DS_Store
tmp/
*.log
*.tmp
*.bak
*.swp
CLAUDE.local.md

# pdh-flow is an independent repo at github.com/masuidrive/pdh-flow.
# Do not let this outer pdh repo track its files (causes nested-repo
# diff noise on every status). Treat it as an opaque sub-checkout.
pdh-flow/

# decision.hanger の発行 client（git 管理外）
decision-board.sh
.decision-board.yaml
evals/private/

# board-check render check の local 依存（scripts/check-board-render.sh 用。配布しない）
scripts/board-check/node_modules/
scripts/board-check/package.json
scripts/board-check/package-lock.json
